//Modules

//When we access the module we use require and assignit to variable name

const SayHi = (name) =>{
    console.log('Hello there' + '' + '' + name);
}
SayHi('Muhluri');

module.exports = {SayHi}